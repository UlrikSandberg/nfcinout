import React from "react";

type User = {
    id: String,
    name: String,
    lastName: String,
    age: Number,
}

const loggedInUsers = []

export const LoginWithNFC = () => {

    // Generate a random nfc login bla bla bla

}
